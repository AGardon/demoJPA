server:
  port: 8080 # ??????vue???vue.config.js??devServer???????
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/online_exam?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
  jpa:
    # ?????????????SQL??(???????),?????logging.level????????SQL??(??+??)
    show-sql: false
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 100MB # ????????
      max-request-size: 100MB # ????????
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
# ????????
interceptors:
  # ??????????????????
  auth-ignore-uris: /api/user/register,/api/user/login

# SQL????(?????????trace????????????????)
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: off

